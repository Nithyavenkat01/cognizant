
// Factory Method Pattern Definition

// A Factory Method Pattern provides an interface for creating objects but lets subclasses decide which object to create.

// In simple words:

// 👉 Instead of creating objects directly using new, we ask a factory to create them.


package com.cognizant.factory;

public interface Document {
    void open();
}